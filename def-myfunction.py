# the function exp is defined below
def exp(x,y):
	z = x**y
	print(z)

base = input('Please enter the base value: ')
exponent = input('Please enter the exponent value: ')

#the function exp is called
exp(base,exponent)